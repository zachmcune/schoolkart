{
  "name": "schoolkart",
  "private": true,
  "description": "SchoolKart Campus Loop — Chromebook arcade F1. One Node host serves the game and in-memory WebSocket rooms.",
  "scripts": {
    "start": "node server/index.js",
    "test": "node server/test-rooms.js && node server/test-ai.js && node server/test-map.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
